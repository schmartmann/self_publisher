const Synchronizer = require('../services/drive/synchronizer')

describe('Synchronizer', () => { 
    it('creates an instance of the synchronizer class', () => {
        const synchronizerInstance = Synchronizer.new()
    });
})